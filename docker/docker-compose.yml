services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: root
    volumes:
      - db-data:/var/lib/postgresql

volumes:
  db-data:
    name: "db-volume"
