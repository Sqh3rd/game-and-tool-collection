import type { Meta } from "@nuxtjs/storybook";
import ProductionNode from "./ProductionNode.vue";

const meta = { component: ProductionNode } satisfies Meta<
  typeof ProductionNode
>;

export default meta;
