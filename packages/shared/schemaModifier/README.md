# schemaModifier

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build schemaModifier` to build the library.

## Running unit tests

Run `nx test schemaModifier` to execute the unit tests via [Vitest](https://vitest.dev/).
